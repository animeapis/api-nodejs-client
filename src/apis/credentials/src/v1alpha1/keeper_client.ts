// Copyright 2022 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

/* global window */
import * as gax from 'google-gax';
import {Callback, CallOptions, Descriptors, ClientOptions, PaginationCallback, GaxCall} from 'google-gax';

import { Transform } from 'stream';
import { RequestType } from 'google-gax/build/src/apitypes';
import * as protos from '../../protos/protos';
import jsonProtos = require('../../protos/protos.json');
/**
 * Client JSON configuration object, loaded from
 * `src/v1alpha1/keeper_client_config.json`.
 * This file defines retry strategy and timeouts for all API methods in this library.
 */
import * as gapicConfig from './keeper_client_config.json';

const version = require('../../../package.json').version;

/**
 * @class
 * @memberof v1alpha1
 */
export class KeeperClient {
  private _terminated = false;
  private _opts: ClientOptions;
  private _gaxModule: typeof gax | typeof gax.fallback;
  private _gaxGrpc: gax.GrpcClient | gax.fallback.GrpcClient;
  private _protos: {};
  private _defaults: {[method: string]: gax.CallSettings};
  auth: gax.GoogleAuth;
  descriptors: Descriptors = {
    page: {},
    stream: {},
    longrunning: {},
    batching: {},
  };
  innerApiCalls: {[name: string]: Function};
  keeperStub?: Promise<{[name: string]: Function}>;

  /**
   * Construct an instance of KeeperClient.
   *
   * @param {object} [options] - The configuration object.
   * The options accepted by the constructor are described in detail
   * in [this document](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#creating-the-client-instance).
   * The common options are:
   * @param {object} [options.credentials] - Credentials object.
   * @param {string} [options.credentials.client_email]
   * @param {string} [options.credentials.private_key]
   * @param {string} [options.email] - Account email address. Required when
   *     using a .pem or .p12 keyFilename.
   * @param {string} [options.keyFilename] - Full path to the a .json, .pem, or
   *     .p12 key downloaded from the Google Developers Console. If you provide
   *     a path to a JSON file, the projectId option below is not necessary.
   *     NOTE: .pem and .p12 require you to specify options.email as well.
   * @param {number} [options.port] - The port on which to connect to
   *     the remote host.
   * @param {string} [options.projectId] - The project ID from the Google
   *     Developer's Console, e.g. 'grape-spaceship-123'. We will also check
   *     the environment variable GCLOUD_PROJECT for your project ID. If your
   *     app is running in an environment which supports
   *     {@link https://developers.google.com/identity/protocols/application-default-credentials Application Default Credentials},
   *     your project ID will be detected automatically.
   * @param {string} [options.apiEndpoint] - The domain name of the
   *     API remote host.
   * @param {gax.ClientConfig} [options.clientConfig] - Client configuration override.
   *     Follows the structure of {@link gapicConfig}.
   * @param {boolean} [options.fallback] - Use HTTP fallback mode.
   *     In fallback mode, a special browser-compatible transport implementation is used
   *     instead of gRPC transport. In browser context (if the `window` object is defined)
   *     the fallback mode is enabled automatically; set `options.fallback` to `false`
   *     if you need to override this behavior.
   */
  constructor(opts?: ClientOptions) {
    // Ensure that options include all the required fields.
    const staticMembers = this.constructor as typeof KeeperClient;
    const servicePath = opts?.servicePath || opts?.apiEndpoint || staticMembers.servicePath;
    const port = opts?.port || staticMembers.port;
    const clientConfig = opts?.clientConfig ?? {};
    const fallback = opts?.fallback ?? (typeof window !== 'undefined' && typeof window?.fetch === 'function');
    opts = Object.assign({servicePath, port, clientConfig, fallback}, opts);

    // If scopes are unset in options and we're connecting to a non-default endpoint, set scopes just in case.
    if (servicePath !== staticMembers.servicePath && !('scopes' in opts)) {
      opts['scopes'] = staticMembers.scopes;
    }

    // Choose either gRPC or proto-over-HTTP implementation of google-gax.
    this._gaxModule = opts.fallback ? gax.fallback : gax;

    // Create a `gaxGrpc` object, with any grpc-specific options sent to the client.
    this._gaxGrpc = new this._gaxModule.GrpcClient(opts);

    // Save options to use in initialize() method.
    this._opts = opts;

    // Save the auth object to the client, for use by other methods.
    this.auth = (this._gaxGrpc.auth as gax.GoogleAuth);

    // Set the default scopes in auth client if needed.
    if (servicePath === staticMembers.servicePath) {
      this.auth.defaultScopes = staticMembers.scopes;
    }

    // Determine the client header string.
    const clientHeader = [
      `gax/${this._gaxModule.version}`,
      `gapic/${version}`,
    ];
    if (typeof process !== 'undefined' && 'versions' in process) {
      clientHeader.push(`gl-node/${process.versions.node}`);
    } else {
      clientHeader.push(`gl-web/${this._gaxModule.version}`);
    }
    if (!opts.fallback) {
      clientHeader.push(`grpc/${this._gaxGrpc.grpcVersion}`);
    } else if (opts.fallback === 'rest' ) {
      clientHeader.push(`rest/${this._gaxGrpc.grpcVersion}`);
    }
    if (opts.libName && opts.libVersion) {
      clientHeader.push(`${opts.libName}/${opts.libVersion}`);
    }
    // Load the applicable protos.
    this._protos = this._gaxGrpc.loadProtoJSON(jsonProtos);

    // Some of the methods on this service return "paged" results,
    // (e.g. 50 results at a time, with tokens to get subsequent
    // pages). Denote the keys used for pagination and results.
    this.descriptors.page = {
      listCredentials:
          new this._gaxModule.PageDescriptor('pageToken', 'nextPageToken', 'credentials')
    };

    // Put together the default options sent with requests.
    this._defaults = this._gaxGrpc.constructSettings(
        'animeshon.credentials.v1alpha1.Keeper', gapicConfig as gax.ClientConfig,
        opts.clientConfig || {}, {'x-goog-api-client': clientHeader.join(' ')});

    // Set up a dictionary of "inner API calls"; the core implementation
    // of calling the API is handled in `google-gax`, with this code
    // merely providing the destination and request information.
    this.innerApiCalls = {};
  }

  /**
   * Initialize the client.
   * Performs asynchronous operations (such as authentication) and prepares the client.
   * This function will be called automatically when any class method is called for the
   * first time, but if you need to initialize it before calling an actual method,
   * feel free to call initialize() directly.
   *
   * You can await on this method if you want to make sure the client is initialized.
   *
   * @returns {Promise} A promise that resolves to an authenticated service stub.
   */
  initialize() {
    // If the client stub promise is already initialized, return immediately.
    if (this.keeperStub) {
      return this.keeperStub;
    }

    // Put together the "service stub" for
    // animeshon.credentials.v1alpha1.Keeper.
    this.keeperStub = this._gaxGrpc.createStub(
        this._opts.fallback ?
          (this._protos as protobuf.Root).lookupService('animeshon.credentials.v1alpha1.Keeper') :
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          (this._protos as any).animeshon.credentials.v1alpha1.Keeper,
        this._opts) as Promise<{[method: string]: Function}>;

    // Iterate over each of the methods that the service provides
    // and create an API call method for each.
    const keeperStubMethods =
        ['getCredentials', 'listCredentials', 'createCredentials', 'deleteCredentials', 'actAsCredentials'];
    for (const methodName of keeperStubMethods) {
      const callPromise = this.keeperStub.then(
        stub => (...args: Array<{}>) => {
          if (this._terminated) {
            return Promise.reject('The client has already been closed.');
          }
          const func = stub[methodName];
          return func.apply(stub, args);
        },
        (err: Error|null|undefined) => () => {
          throw err;
        });

      const descriptor =
        this.descriptors.page[methodName] ||
        undefined;
      const apiCall = this._gaxModule.createApiCall(
        callPromise,
        this._defaults[methodName],
        descriptor
      );

      this.innerApiCalls[methodName] = apiCall;
    }

    return this.keeperStub;
  }

  /**
   * The DNS address for this API service.
   * @returns {string} The DNS address for this service.
   */
  static get servicePath() {
    return 'credentials.animeapis.com';
  }

  /**
   * The DNS address for this API service - same as servicePath(),
   * exists for compatibility reasons.
   * @returns {string} The DNS address for this service.
   */
  static get apiEndpoint() {
    return 'credentials.animeapis.com';
  }

  /**
   * The port for this API service.
   * @returns {number} The default port for this service.
   */
  static get port() {
    return 443;
  }

  /**
   * The scopes needed to make gRPC calls for every method defined
   * in this service.
   * @returns {string[]} List of default scopes.
   */
  static get scopes() {
    return [];
  }

  getProjectId(): Promise<string>;
  getProjectId(callback: Callback<string, undefined, undefined>): void;
  /**
   * Return the project ID used by this class.
   * @returns {Promise} A promise that resolves to string containing the project ID.
   */
  getProjectId(callback?: Callback<string, undefined, undefined>):
      Promise<string>|void {
    if (callback) {
      this.auth.getProjectId(callback);
      return;
    }
    return this.auth.getProjectId();
  }

  // -------------------
  // -- Service calls --
  // -------------------
  getCredentials(
      request?: protos.animeshon.credentials.v1alpha1.IGetCredentialsRequest,
      options?: CallOptions):
      Promise<[
        protos.animeshon.credentials.v1alpha1.ICredentials,
        protos.animeshon.credentials.v1alpha1.IGetCredentialsRequest|undefined, {}|undefined
      ]>;
  getCredentials(
      request: protos.animeshon.credentials.v1alpha1.IGetCredentialsRequest,
      options: CallOptions,
      callback: Callback<
          protos.animeshon.credentials.v1alpha1.ICredentials,
          protos.animeshon.credentials.v1alpha1.IGetCredentialsRequest|null|undefined,
          {}|null|undefined>): void;
  getCredentials(
      request: protos.animeshon.credentials.v1alpha1.IGetCredentialsRequest,
      callback: Callback<
          protos.animeshon.credentials.v1alpha1.ICredentials,
          protos.animeshon.credentials.v1alpha1.IGetCredentialsRequest|null|undefined,
          {}|null|undefined>): void;
/**
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.name
 *   The name of the credentials to retrieve.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [Credentials]{@link animeshon.credentials.v1alpha1.Credentials}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.getCredentials(request);
 */
  getCredentials(
      request?: protos.animeshon.credentials.v1alpha1.IGetCredentialsRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.animeshon.credentials.v1alpha1.ICredentials,
          protos.animeshon.credentials.v1alpha1.IGetCredentialsRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.animeshon.credentials.v1alpha1.ICredentials,
          protos.animeshon.credentials.v1alpha1.IGetCredentialsRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.animeshon.credentials.v1alpha1.ICredentials,
        protos.animeshon.credentials.v1alpha1.IGetCredentialsRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'name': request.name || '',
    });
    this.initialize();
    return this.innerApiCalls.getCredentials(request, options, callback);
  }
  createCredentials(
      request?: protos.animeshon.credentials.v1alpha1.ICreateCredentialsRequest,
      options?: CallOptions):
      Promise<[
        protos.animeshon.credentials.v1alpha1.ICredentials,
        protos.animeshon.credentials.v1alpha1.ICreateCredentialsRequest|undefined, {}|undefined
      ]>;
  createCredentials(
      request: protos.animeshon.credentials.v1alpha1.ICreateCredentialsRequest,
      options: CallOptions,
      callback: Callback<
          protos.animeshon.credentials.v1alpha1.ICredentials,
          protos.animeshon.credentials.v1alpha1.ICreateCredentialsRequest|null|undefined,
          {}|null|undefined>): void;
  createCredentials(
      request: protos.animeshon.credentials.v1alpha1.ICreateCredentialsRequest,
      callback: Callback<
          protos.animeshon.credentials.v1alpha1.ICredentials,
          protos.animeshon.credentials.v1alpha1.ICreateCredentialsRequest|null|undefined,
          {}|null|undefined>): void;
/**
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {animeshon.credentials.v1alpha1.Credentials} request.credentials
 *   The credentials to create.
 * @param {animeshon.credentials.v1alpha1.CreateCredentialsRequest.Basic} request.basic
 *   Basic authentication credentials composed by username and password.
 * @param {animeshon.credentials.v1alpha1.CreateCredentialsRequest.OAuth2} request.oauth2
 *   OAuth 2.0 authentication credentials composed by a refresh token.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [Credentials]{@link animeshon.credentials.v1alpha1.Credentials}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.createCredentials(request);
 */
  createCredentials(
      request?: protos.animeshon.credentials.v1alpha1.ICreateCredentialsRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.animeshon.credentials.v1alpha1.ICredentials,
          protos.animeshon.credentials.v1alpha1.ICreateCredentialsRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.animeshon.credentials.v1alpha1.ICredentials,
          protos.animeshon.credentials.v1alpha1.ICreateCredentialsRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.animeshon.credentials.v1alpha1.ICredentials,
        protos.animeshon.credentials.v1alpha1.ICreateCredentialsRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'credentials.name': request.credentials!.name || '',
    });
    this.initialize();
    return this.innerApiCalls.createCredentials(request, options, callback);
  }
  deleteCredentials(
      request?: protos.animeshon.credentials.v1alpha1.IDeleteCredentialsRequest,
      options?: CallOptions):
      Promise<[
        protos.google.protobuf.IEmpty,
        protos.animeshon.credentials.v1alpha1.IDeleteCredentialsRequest|undefined, {}|undefined
      ]>;
  deleteCredentials(
      request: protos.animeshon.credentials.v1alpha1.IDeleteCredentialsRequest,
      options: CallOptions,
      callback: Callback<
          protos.google.protobuf.IEmpty,
          protos.animeshon.credentials.v1alpha1.IDeleteCredentialsRequest|null|undefined,
          {}|null|undefined>): void;
  deleteCredentials(
      request: protos.animeshon.credentials.v1alpha1.IDeleteCredentialsRequest,
      callback: Callback<
          protos.google.protobuf.IEmpty,
          protos.animeshon.credentials.v1alpha1.IDeleteCredentialsRequest|null|undefined,
          {}|null|undefined>): void;
/**
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.name
 *   The name of the credentials to delete.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [Empty]{@link google.protobuf.Empty}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.deleteCredentials(request);
 */
  deleteCredentials(
      request?: protos.animeshon.credentials.v1alpha1.IDeleteCredentialsRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.google.protobuf.IEmpty,
          protos.animeshon.credentials.v1alpha1.IDeleteCredentialsRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.google.protobuf.IEmpty,
          protos.animeshon.credentials.v1alpha1.IDeleteCredentialsRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.google.protobuf.IEmpty,
        protos.animeshon.credentials.v1alpha1.IDeleteCredentialsRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'name': request.name || '',
    });
    this.initialize();
    return this.innerApiCalls.deleteCredentials(request, options, callback);
  }
  actAsCredentials(
      request?: protos.animeshon.credentials.v1alpha1.IActAsCredentialsRequest,
      options?: CallOptions):
      Promise<[
        protos.animeshon.credentials.v1alpha1.IActAsCredentialsResponse,
        protos.animeshon.credentials.v1alpha1.IActAsCredentialsRequest|undefined, {}|undefined
      ]>;
  actAsCredentials(
      request: protos.animeshon.credentials.v1alpha1.IActAsCredentialsRequest,
      options: CallOptions,
      callback: Callback<
          protos.animeshon.credentials.v1alpha1.IActAsCredentialsResponse,
          protos.animeshon.credentials.v1alpha1.IActAsCredentialsRequest|null|undefined,
          {}|null|undefined>): void;
  actAsCredentials(
      request: protos.animeshon.credentials.v1alpha1.IActAsCredentialsRequest,
      callback: Callback<
          protos.animeshon.credentials.v1alpha1.IActAsCredentialsResponse,
          protos.animeshon.credentials.v1alpha1.IActAsCredentialsRequest|null|undefined,
          {}|null|undefined>): void;
/**
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.name
 *   The resorce name of the credentials.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is an object representing [ActAsCredentialsResponse]{@link animeshon.credentials.v1alpha1.ActAsCredentialsResponse}.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods)
 *   for more details and examples.
 * @example
 * const [response] = await client.actAsCredentials(request);
 */
  actAsCredentials(
      request?: protos.animeshon.credentials.v1alpha1.IActAsCredentialsRequest,
      optionsOrCallback?: CallOptions|Callback<
          protos.animeshon.credentials.v1alpha1.IActAsCredentialsResponse,
          protos.animeshon.credentials.v1alpha1.IActAsCredentialsRequest|null|undefined,
          {}|null|undefined>,
      callback?: Callback<
          protos.animeshon.credentials.v1alpha1.IActAsCredentialsResponse,
          protos.animeshon.credentials.v1alpha1.IActAsCredentialsRequest|null|undefined,
          {}|null|undefined>):
      Promise<[
        protos.animeshon.credentials.v1alpha1.IActAsCredentialsResponse,
        protos.animeshon.credentials.v1alpha1.IActAsCredentialsRequest|undefined, {}|undefined
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'name': request.name || '',
    });
    this.initialize();
    return this.innerApiCalls.actAsCredentials(request, options, callback);
  }

  listCredentials(
      request?: protos.animeshon.credentials.v1alpha1.IListCredentialsRequest,
      options?: CallOptions):
      Promise<[
        protos.animeshon.credentials.v1alpha1.ICredentials[],
        protos.animeshon.credentials.v1alpha1.IListCredentialsRequest|null,
        protos.animeshon.credentials.v1alpha1.IListCredentialsResponse
      ]>;
  listCredentials(
      request: protos.animeshon.credentials.v1alpha1.IListCredentialsRequest,
      options: CallOptions,
      callback: PaginationCallback<
          protos.animeshon.credentials.v1alpha1.IListCredentialsRequest,
          protos.animeshon.credentials.v1alpha1.IListCredentialsResponse|null|undefined,
          protos.animeshon.credentials.v1alpha1.ICredentials>): void;
  listCredentials(
      request: protos.animeshon.credentials.v1alpha1.IListCredentialsRequest,
      callback: PaginationCallback<
          protos.animeshon.credentials.v1alpha1.IListCredentialsRequest,
          protos.animeshon.credentials.v1alpha1.IListCredentialsResponse|null|undefined,
          protos.animeshon.credentials.v1alpha1.ICredentials>): void;
/**
 *
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent, which owns this collection of credentials.
 * @param {number} request.pageSize
 *   If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   The value returned from the previous call.
 * @param {string} request.filter
 *   A filter to be applied to results.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Promise} - The promise which resolves to an array.
 *   The first element of the array is Array of [Credentials]{@link animeshon.credentials.v1alpha1.Credentials}.
 *   The client library will perform auto-pagination by default: it will call the API as many
 *   times as needed and will merge results from all the pages into this array.
 *   Note that it can affect your quota.
 *   We recommend using `listCredentialsAsync()`
 *   method described below for async iteration which you can stop as needed.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 */
  listCredentials(
      request?: protos.animeshon.credentials.v1alpha1.IListCredentialsRequest,
      optionsOrCallback?: CallOptions|PaginationCallback<
          protos.animeshon.credentials.v1alpha1.IListCredentialsRequest,
          protos.animeshon.credentials.v1alpha1.IListCredentialsResponse|null|undefined,
          protos.animeshon.credentials.v1alpha1.ICredentials>,
      callback?: PaginationCallback<
          protos.animeshon.credentials.v1alpha1.IListCredentialsRequest,
          protos.animeshon.credentials.v1alpha1.IListCredentialsResponse|null|undefined,
          protos.animeshon.credentials.v1alpha1.ICredentials>):
      Promise<[
        protos.animeshon.credentials.v1alpha1.ICredentials[],
        protos.animeshon.credentials.v1alpha1.IListCredentialsRequest|null,
        protos.animeshon.credentials.v1alpha1.IListCredentialsResponse
      ]>|void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    }
    else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    this.initialize();
    return this.innerApiCalls.listCredentials(request, options, callback);
  }

/**
 * Equivalent to `method.name.toCamelCase()`, but returns a NodeJS Stream object.
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent, which owns this collection of credentials.
 * @param {number} request.pageSize
 *   If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   The value returned from the previous call.
 * @param {string} request.filter
 *   A filter to be applied to results.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Stream}
 *   An object stream which emits an object representing [Credentials]{@link animeshon.credentials.v1alpha1.Credentials} on 'data' event.
 *   The client library will perform auto-pagination by default: it will call the API as many
 *   times as needed. Note that it can affect your quota.
 *   We recommend using `listCredentialsAsync()`
 *   method described below for async iteration which you can stop as needed.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 */
  listCredentialsStream(
      request?: protos.animeshon.credentials.v1alpha1.IListCredentialsRequest,
      options?: CallOptions):
    Transform{
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    const callSettings = new gax.CallSettings(options);
    this.initialize();
    return this.descriptors.page.listCredentials.createStream(
      this.innerApiCalls.listCredentials as gax.GaxCall,
      request,
      callSettings
    );
  }

/**
 * Equivalent to `listCredentials`, but returns an iterable object.
 *
 * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
 * @param {Object} request
 *   The request object that will be sent.
 * @param {string} request.parent
 *   The parent, which owns this collection of credentials.
 * @param {number} request.pageSize
 *   If unspecified, server will pick an appropriate default.
 * @param {string} request.pageToken
 *   The value returned from the previous call.
 * @param {string} request.filter
 *   A filter to be applied to results.
 * @param {object} [options]
 *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
 * @returns {Object}
 *   An iterable Object that allows [async iteration](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols).
 *   When you iterate the returned iterable, each element will be an object representing
 *   [Credentials]{@link animeshon.credentials.v1alpha1.Credentials}. The API will be called under the hood as needed, once per the page,
 *   so you can stop the iteration when you don't need more results.
 *   Please see the
 *   [documentation](https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination)
 *   for more details and examples.
 * @example
 * const iterable = client.listCredentialsAsync(request);
 * for await (const response of iterable) {
 *   // process response
 * }
 */
  listCredentialsAsync(
      request?: protos.animeshon.credentials.v1alpha1.IListCredentialsRequest,
      options?: CallOptions):
    AsyncIterable<protos.animeshon.credentials.v1alpha1.ICredentials>{
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers[
      'x-goog-request-params'
    ] = gax.routingHeader.fromParams({
      'parent': request.parent || '',
    });
    options = options || {};
    const callSettings = new gax.CallSettings(options);
    this.initialize();
    return this.descriptors.page.listCredentials.asyncIterate(
      this.innerApiCalls['listCredentials'] as GaxCall,
      request as unknown as RequestType,
      callSettings
    ) as AsyncIterable<protos.animeshon.credentials.v1alpha1.ICredentials>;
  }

  /**
   * Terminate the gRPC channel and close the client.
   *
   * The client will no longer be usable and all future behavior is undefined.
   * @returns {Promise} A promise that resolves when the client is closed.
   */
  close(): Promise<void> {
    this.initialize();
    if (!this._terminated) {
      return this.keeperStub!.then(stub => {
        this._terminated = true;
        stub.close();
      });
    }
    return Promise.resolve();
  }
}
